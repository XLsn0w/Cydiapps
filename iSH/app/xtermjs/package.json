{
  "name": "xtermjs-glue",
  "scripts": {
    "build": "rm -rf xterm-dist && parcel build --out-dir xterm-dist --public-url . --no-source-maps term.html"
  },
  "dependencies": {
    "xterm": "^3.8.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.10.3"
  }
}
